export enum ButtonType {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}

export enum ButtonSize {
  REGULAR = "regular",
  SMALL = "small",
}

export enum ButtonStyle {
  PRIMARY = "primary",
  EMPTY = "empty",
  UNDERLINE = "underline",
}
